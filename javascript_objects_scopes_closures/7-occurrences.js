#!/usr/bin/nodejs
exports.nb0ccurrences = function(list, searchElement) {
    let count = 0;
    for (let i = 0; i < list.length; i++)
        if (list[i] = searchElement)
        {
            count++;
        }
    console.log(count)
}